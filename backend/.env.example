# Lapa Casa Hostel - Channel Manager
# Copiar a .env y completar con los valores reales.

# Postgres (Supabase en produccion; local/Docker en desarrollo)
DATABASE_URL=postgresql://lapa_dev:lapa_dev_pw@localhost:5432/lapa_casa_hostel

# El resto de las variables (Redis/Upstash, Stripe, MercadoPago, JWT, Resend)
# se agregan en las ventanas siguientes, a medida que cada integracion se
# construye (ver Prompt Maestro, seccion TECNOLOGIAS A USAR).
