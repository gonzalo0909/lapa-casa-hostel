// frontend/src/components/booking/apartment-engine.types.ts
// Tipos compartidos del motor de reservas de apartamentos.

/** Locales soportados por el motor de apartamentos. */
export type AptLocale = 'pt' | 'es' | 'en' | 'fr' | 'de';
